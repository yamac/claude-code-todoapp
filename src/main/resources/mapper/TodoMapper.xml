<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todo.mapper.TodoMapper">
    
    <resultMap id="TodoResultMap" type="com.example.todo.model.Todo">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="completed" column="completed"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <select id="findAll" resultMap="TodoResultMap">
        SELECT id, title, description, completed, created_at, updated_at
        FROM todos
        ORDER BY created_at DESC
    </select>
    
    <select id="findById" parameterType="long" resultMap="TodoResultMap">
        SELECT id, title, description, completed, created_at, updated_at
        FROM todos
        WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.example.todo.model.Todo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (title, description, completed, created_at, updated_at)
        VALUES (#{title}, #{description}, #{completed}, #{createdAt}, #{updatedAt})
    </insert>
    
    <update id="update" parameterType="com.example.todo.model.Todo">
        UPDATE todos
        SET title = #{title},
            description = #{description},
            completed = #{completed},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="long">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>
    
</mapper>